scikit-learn>=1.3
numpy>=1.24,<2
pymupdf>=1.24

# deep-learning stack (match torch to your CUDA driver, e.g. cu121 for driver 12.5)
torch
transformers
sentencepiece
peft                  # LoRA adapters (rrl-experiments tierC_lora)
